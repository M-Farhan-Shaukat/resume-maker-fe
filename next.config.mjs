/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {},
  images: {
    domains: ["i.ibb.co", "s3-us-west-2.amazonaws.com"],
  },
};

export default nextConfig;
